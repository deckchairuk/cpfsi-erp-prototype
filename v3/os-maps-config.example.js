// Copy to os-maps-config.local.js and add your OS Data Hub Project API key.
// Enable "OS Maps API" on the project in https://osdatahub.os.uk/
// The API secret is for server-side use only — browser maps need the key below.
window.OS_MAPS_CONFIG = {
  apiKey: 'YOUR_OS_PROJECT_API_KEY'
};
